# install reports stages

    Code
      install(pkg, reload = FALSE, build = TRUE)
    Output
      -- Installing dependencies of testTest ---------------------
    Message
    Output
      -- R CMD build ---------------------------------------------
      -- Installing testTest -------------------------------------

