# Return a vector of names of attached packages

Return a vector of names of attached packages

## Usage

``` r
loaded_packages()
```

## Value

A data frame with columns package and path, giving the name of each
package and the path it was loaded from.
